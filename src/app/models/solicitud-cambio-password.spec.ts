import { SolicitudCambioPassword } from './solicitud-cambio-password';

describe('SolicitudCambioPassword', () => {
  it('should create an instance', () => {
    expect(new SolicitudCambioPassword()).toBeTruthy();
  });
});
